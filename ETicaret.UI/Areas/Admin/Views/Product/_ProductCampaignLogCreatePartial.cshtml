@model List<ETicaret.UI.Areas.Admin.Models.ProductCampaignLogVMs.ProductCampaignLogListVM>

@* <div class="col-lg-12">
    <div class="card mb-4">
        <div class="card-header">
            <h4 class="card-heading">Product Campaign Logs</h4>
        </div>
        <div class="card-body"> *@
            <table id="tb1" class="table table-striped table-hover card-text">
                <thead>
                    <tr class="tr-shrink">
                        <th>#</th>
                        <th>Campaign Name</th>
                        <th>Start Date</th>
                        <th>End Date</th>
                        <th>Discount Percentage</th>
                    </tr>
                </thead>
                <tbody>
                    @for (var i = 0; i < Model.Count; i++)
                    {
                        <tr class="tr-shrink">
                            <th scope="row">@(i + 1)</th>
                            <td>
                                <span data-bs-toggle="tooltip" title="@Model[i].CampaignName">
                                    @Html.DisplayFor(modelItem => Model[i].CampaignName)
                                </span>
                            </td>
                            <td>
                                <span data-bs-toggle="tooltip" title="@Model[i].StartDate.ToString("dd/MM/yyyy")">
                                    @Html.DisplayFor(modelItem => Model[i].StartDate)
                                </span>
                            </td>
                            <td>
                                <span data-bs-toggle="tooltip" title="@Model[i].EndDate.ToString("dd/MM/yyyy")">
                                    @Html.DisplayFor(modelItem => Model[i].EndDate)
                                </span>
                            </td>
                            <td>
                                <span data-bs-toggle="tooltip" title="@Model[i].DiscountPercentage.ToString() + '%'">
                                    @Html.DisplayFor(modelItem => Model[i].DiscountPercentage)
                                </span>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        @* </div>
    </div>
</div> *@
